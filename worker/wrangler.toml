name = "coveted-votes"
main = "src/index.js"
compatibility_date = "2026-05-01"

# KV binding stays managed by the Cloudflare dashboard.
# (Settings → Variables → KV Namespace Bindings → VOTES)
# ADMIN_SECRET stays as an encrypted Variable in the same screen.
